<% content_for :title, t("zones.index.title") %>

<div class="mb-6 flex items-center gap-2 text-sm text-gray-500">
  <%= link_to t("zones.index.title"), zones_path, class: "hover:underline" %>
  <span>/</span>
  <span class="text-gray-900"><%= t("zones.new.title") %></span>
</div>

<div class="bg-white rounded-lg border border-gray-200 p-6">
  <h1 class="text-xl font-semibold text-gray-900 mb-4"><%= t("zones.new.title") %></h1>
  <%= render "form", zone: @zone %>
</div>
