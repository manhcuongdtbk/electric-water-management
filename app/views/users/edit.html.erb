<% content_for :title, t("users.edit.title") %>

<div class="mb-6 flex items-center gap-2 text-sm text-gray-500">
  <%= link_to t("users.index.title"), users_path, class: "hover:underline" %>
  <span>/</span>
  <span class="text-gray-900"><%= t("users.edit.title") %></span>
</div>

<div class="bg-white rounded-lg border border-gray-200 p-6 max-w-lg">
  <h1 class="text-xl font-semibold text-gray-900 mb-2"><%= t("users.edit.title") %></h1>
  <p class="text-sm text-gray-500 mb-6"><%= @user.full_name %> &lt;<%= @user.email %>&gt;</p>
  <%= render "form", user: @user, unit_organizations: @unit_organizations %>
</div>
