<% content_for :title, t("organizations.edit.title") %>

<div class="mb-6">
  <h1 class="text-xl font-semibold text-gray-900"><%= t("organizations.edit.title") %></h1>
</div>

<%= render "form", organization: @organization %>
